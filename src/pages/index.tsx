import type { NextPage } from 'next';
import { Init } from '../components';

const Home: NextPage = () => {
    return (
        <div>
            <Init>init</Init>
        </div>
    );
};

export default Home;
